    // hash function to map values to key
///////////This hash function could be implemented in an another way,, you can choose how to evaluate the index
    int hashFunction(int x) 
    {
        return (x % Size);
    }
